<!DOCTYPE html>
<html lang=“en”>

<head>
  <meta charset="utf-8">
  <title>TinyApp</title>
</head>


<body>

  <%- include('partials/_header') %>

    
    <br/>
    <% if (user) { %>
    <% for (var key in urls) { %>
            <li>
              <%= [key] %> : <%= urls[key] %>
              <a href="/urls/<%=key%>"/>Update</a> | <a href="/urls/<%=key%>/delete">Delete</a>
            </li>
    <% } %>   
      <br/>
      <a href="/urls/new">Create a New Short Link</a>
    <%  } else { %>
      <p>To access this page (/urls) you must log in.</p>
    <% } %>
</body>

</html>